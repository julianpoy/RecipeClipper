export const matchYield = /(serves|servings|yield):?\s*\d+/i;

export const matchActiveTime = /(active time|prep time):?\s*(\d+ (hour(s?)|hr(s?)|minute(s?)|min(s?))? ?(and)? ?)+/i;

export const matchTotalTime = /(total time):?\s*(\d+ (hour(s?)|hr(s?)|minute(s?)|min(s?))? ?(and)? ?)+/i;
